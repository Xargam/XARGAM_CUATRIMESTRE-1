#include <stdio.h>
#include <stdlib.h>

int obtenerMaximo(int num, int max);

int main()
{
    int numero;
    int maximo;
    int respuesta;

    while(respuesta != 10)
    {
        printf("Ingrese un numero:\n");
        scanf("%d");
        obtener
    }

    return 0;
}

int
