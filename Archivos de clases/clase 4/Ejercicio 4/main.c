#include <stdio.h>
#include <stdlib.h>
#define CANT 5

int main()
{
    int vector[CANT],estado[CANT], i;
    char respuesta = 's';

    for(i = 0; i < CANT; i++)
        vector[i] = 0;

    do
    {
        for(i = 0; i < CANT; I++)
        printf("Ingrese valor:\n");
        scanf("%d",vector[i]);

        printf("Seguir? S/N");
        respuesta = tolower(getch());
    }
    while(respuesta = 's')

    for(vector)





    return 0;
}
